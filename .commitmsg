feat(tools): add Jupiter/Raydium on-chain AI tools + fix 11 Jupiter REST bugs

Added:
- Jupiter On-Chain tools (10): local RPC-based program state queries
- Raydium On-Chain tools (10): local RPC-based pool/farm queries
- Configurable apiKeyHeader on ProtocolClientConfig
- Super-factory wires 5 protocol toolkits (68 total tools)

Fixed (Jupiter REST #4-#14):
- Auth header x-api-key instead of Authorization Bearer
- getTokenList routes to tokens.jup.ag/tokens
- Removed getRouters (404 endpoint), methods 22 to 20
- getHoldings uses tokenAccountAddresses array
- getLimitOrders/getDCAOrders add orderStatus + recurringType
- createLimitOrder wraps fields in params object
- executeTrigger/executeDCA add requestId
- createDCA/cancelDCA rename maker to user

Tests: 415 passed, 0 failed
